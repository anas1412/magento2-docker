[supervisord]
nodaemon=true

[program:apache2]
command=/bin/bash -c "source /etc/apache2/envvars && exec /usr/sbin/apache2 -DFOREGROUND"

#[program:deploy_magento]
#command=/bin/bash -c "chmod a+x /etc/deploy.sh; /etc/deploy.sh"